export type StatsFightType = {
  user_name: string | null;
  full_name: string;
  hide_id: string;
  user_level: number;
  user_rating: number;
  fights: number;
  win: number;
  loos: number;
  // include the types of the other fields if you need them
};
